# PHP Documentation Guidelines

1. **Use PHPDoc:** Document classes, methods, and properties using `/** ... */`.
2. **Type Safety:** Use `@param` and `@return` to indicate types, especially in legacy codebases without strong scalar typing.
3. **Exceptions:** Document potential exceptions with `@throws`.
