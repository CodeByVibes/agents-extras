# Java Documentation Guidelines

1. **Use Javadoc:** Document all public and protected methods/classes using `/** ... */`.
2. **Tags:** Always include `@param`, `@return`, and `@throws` where applicable.
3. **First Sentence:** Make the first sentence a concise summary, as Javadoc uses this in the summary tables.
