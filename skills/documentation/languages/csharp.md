# C# Documentation Guidelines

1. **Use XML Comments:** Document code using triple-slash XML comments (`///`).
2. **Tags:** Use `<summary>`, `<param>`, `<returns>`, and `<exception>`.
3. **Code References:** Use `<see cref="MethodName"/>` to link to other code elements securely.
