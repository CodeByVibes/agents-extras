# JavaScript Documentation Guidelines

1. **Use JSDoc:** Document all functions and classes using JSDoc (`/** ... */`).
2. **Type Declarations:** Since JavaScript is dynamically typed, always use `@param {Type}` and `@returns {Type}` to explicitly define expected types.
3. **Complex Objects:** Document complex object structures using `@typedef` and `@property`.
