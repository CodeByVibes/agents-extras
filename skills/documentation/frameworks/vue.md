# Vue Documentation Guidelines

1. **SFC Documentation:** Document Single File Components (SFCs) using comments at the top of the `<script>` tag.
2. **Props, Emits, Slots:** Clearly document accepted props, emitted events, and available slots.
3. **VueDoc:** Consider using tools like `vuedoc` to auto-generate documentation from SFC comments.
