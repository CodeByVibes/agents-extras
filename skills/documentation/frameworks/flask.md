# Flask Documentation Guidelines

1. **Route Documentation:** Use docstrings on route handler functions to explain expected request methods, payloads, and response shapes.
2. **Blueprints:** Document what each Blueprint is responsible for at the module level.
3. **Decorators:** If writing custom decorators (e.g., for auth), document exactly what context or requirements they enforce.
